# Change Log — 2026-06-19

> Aturan: setiap update tulis di sini (lihat `CLAUDE.md`). Koreksi data juga tercatat di tab `Change_Log` Sheets.

## Audit konsistensi sumber data chart
- **Temuan:** "Obtained" dihitung lewat 2 jalur — **cycles** (`canonicalObtained`, untuk KPI/total) vs **stats** (`getObtainedByProdAgg` = util+avail, untuk breakdown per-produk). Beberapa visual menaruh total (cycles) di atas breakdown (stats) → bisa menyimpang.
- **Cek live:** per-company cycles vs stats **cocok di semua 41 company** (0 mismatch) → data sudah konsisten, **tidak perlu sinkronisasi data**.
- **Util** konsisten dari lot; **Realized (kartu utama)** dari `ra_records.berat`. "Realized" punya 3 sumber di kartu berbeda (RA / lot realMT / realizationByProd) — inkonsistensi sekunder, dicatat untuk nanti.

## Perbaikan + deploy
### Chart "Obtained vs Utilization" — strip & garis satu sumber
- **Ubah:** strip ringkasan (Total Obtained/Utilized/Remaining) kini dihitung dari `allRecords` yang sama dengan batang & garis yang di-plot — sebelumnya strip pakai `canonicalObtainedFiltered` (cycles) sementara batang pakai stats (sama hanya kebetulan).
- **File:** `public/js/17-ou-chart.js` (strip ~256-268), `public/index.html` (bump `17-ou-chart.js?v=4`).
- **Commit/Deploy:** `92c1fa1` · Heroku **v80** · origin synced.
- **Verifikasi:** halaman render, 0 error console; KPI 24.680 / 17.260 / 7.420 konsisten; cycles=stats=24.680 (live).

### Rule changelog
- **Ubah:** tambah `CLAUDE.md` (rule: tiap update → `logs/{date}_log.md`) + folder `logs/`.
- **Commit:** `92c1fa1`.

### Drift guard read-only (Obtained: cycles vs stats)
- **Ubah:** tambah pengecek read-only di akhir `loadData` — bandingkan `canonicalObtained` (cycles) vs Σ `getObtainedByProdAgg` (stats) per company; `console.warn` bila menyimpang. `window.__auditObtained()` untuk cek manual. Dibungkus try/catch (tak pernah memecah loadData). **Tidak** mengubah data/sumber/pipeline.
- **File:** `public/js/01-data.js` (akhir loadData), bump `?v=20`.
- **Commit/Deploy:** `e85ad5b` · Heroku **v82** · origin synced.
- **Verifikasi:** file ter-serve memuat guard; logika dijalankan inline di browser → 0 drift / 41 company (CONSISTENT). Catatan: browser yang terbuka saat deploy bisa men-cache `?v=20` lama (HTTP cache) → hard-refresh sekali untuk dapat guard.

### Fix #1 — Manual-Update Obtained reconciles stats (audit modal Input Data)
- **Ubah:** edit "Obtained MT per produk" (non-revisi) dulu hanya update `companies.obtained`+`cycle_products`, **tidak** `company_product_stats` → drift KPI vs breakdown. Sekarang `saveEdit` kirim `body.obtainedStats` (obtained per-produk) untuk edit non-revisi; `patchCompanySheets` set `available = max(0, obtained − util)` (util dipertahankan), lalu recompute total company. Deklaratif + idempoten; tanpa derivasi cycles; tanpa logika revisi (revisi tetap via `record-obtained`).
- **File:** `server.js` (patchCompanySheets), `public/js/13-rev-mgmt.js` (saveEdit), `public/js/16-storage.js` (patchToServer), bump `13?v=7` `16?v=7`.
- **Commit/Deploy:** `f96d1ac` · Heroku **v84** · origin synced.
- **Verifikasi (live, idempoten+revert):** AADC obtained=150 (sama) → tak berubah; ADD 150→200 → util 150 dipertahankan, avail 50; REVERT →200→150 → u150 a0 (pulih persis). HTTP 200 semua.
- **Aman per-role (dari kode):** `obtainedProdTable` hanya CorpSec/SuperAdmin (`10-edit-form.js:76-82`). Sales/Operations `canObtained=false` → `obtainedStats` tak dikirim → save mereka tak berubah. Role-gating dua lapis (UI `applyRolePermissions` + save `can()`).

### Fix #5 + #3 (cleanup modal Input Data)
- **#5 (eUtilMT):** hapus jalur override util manual di `saveEdit` (cabang no-shipments) — dulu `eUtilMT` (SuperAdmin) bisa menimpa `co.utilizationMT` (clobber nilai dari lot). Sekarang util tetap server-reconciled; `availableQuota` tetap dijaga konsisten. File `public/js/13-rev-mgmt.js`, bump `?v=8`.
- **#3 (label Manage Data):** banner "Auto-Save"/"Save Now"/"Reset All" menyesatkan (localStorage saja). Diubah jujur → "Tersimpan ke server saat Save", tombol "💾 Backup Lokal" + "↺ Hapus Cache Lokal" (+tooltip), footer "Sumber data: Google Sheets (live)". Fungsi tombol tidak diubah. File `public/index.html`.
- **Commit/Deploy:** `64355f0` · Heroku **v86** · origin synced. Verifikasi live: label baru tampil; override `co.utilizationMT = newUtilMT` hilang dari file ter-serve.
- **#6 (eTarget→ra.target) & #7 (obtained merge tak-hapus): SENGAJA DIBIARKAN** — perilaku sekarang lebih aman (merge tidak menghapus; `collectProductMTs` mengecualikan input kosong → replace bisa menghilangkan produk). "Fix"-nya berisiko hilang data.

### Temuan dilaporkan (TIDAK diubah — hindari rewire berisiko)
- **"Realized" 3 sumber beda:** RA.berat=3.790,38 (KPI resmi) · lot realMT=466,13 (kartu "Obtain vs Realization") · realizationByProd=0 (tak terpakai). Sebagian beda lensa by-design; menyatukan = rewire sumber kartu → ditunda atas instruksi "jangan buat pipeline baru yang kacau".
- Sisa jalur Obtained dual-source (KPI2↔modal, AVQ KPI↔grid, All-Companies baris↔sub-baris) dibiarkan: angka benar sekarang, dijaga `record-obtained` + guard di atas.

## Konteks deploy hari-hari sebelumnya (referensi)
- v74 fix wipe tab companies · v75 self-host Chart.js+xlsx · v76 lazy-load xlsx + DOMContentLoaded · v77 fix util-zeroing (lot util=0) · `a5ef498` fitur "📌 Catat Terbit" (`POST /record-obtained`, obtained baru otomatis masuk KPI+Available).
- Koreksi data: AMP (avail 10→0), SJH/LCP/BBB (Obtained #2 terbit + available), restore utilisasi 3.620 MT dari Neon (BTS/SGD), SNSD (hapus GL Boron → GI Boron 3000).

## HH:MM WIB — Koreksi realisasi 4 PT (CGK/GKL/KAN/NCT)
- **Ubah (data-only):** input realisasi Operations untuk 4 PT (GI BORON, pib 19/06) tadi menimpa realisasi lama CGK/GKL. Per instruksi user (non-duplikat→tambah, duplikat→timpa): CGK 205,8→**983,19** (777,39+205,8), GKL 90,49→**721,26** (630,77+90,49), KAN **77,23** & NCT **149,35** (timpa estimasi belum-tiba). Semua RA `cargo_arrived=true` + lot ditandai tiba.
- **Tab/Sheet:** `ra_records` (berat/cargo_arrived/real_pct), `company_shipments` (cargo_arrived), `Change_Log`.
- **Commit/Deploy:** data-only (tanpa deploy).
- **Alasan:** realisasi lama (Neon) bukan duplikat shipment baru → dijumlahkan. Verifikasi live: TOTAL REALIZED 2.905 → **4.313,25** (11 company).
- **Pending:** drill-down per-PT di tab Util & Realization (task #1) — sedang dikerjakan.

## HH:MM WIB — Drawer: detail realisasi per-lot (task #1)
- **Ubah:** drawer dulu hanya menampilkan detail realisasi dari PIB import (`REALIZATION_SUMMARY`/`/api/realizations`) → PT yang realisasinya via Operations (lot realMT) tak punya detail saat di-klik dari tab Util & Realization. Ditambah tabel read-only **"Realization — per Shipment/Lot"** (produk · lot · realized MT · PIB date · status tiba) dari `co.shipments`.
- **File:** `public/js/08-drawer.js`, bump `?v=12`.
- **Commit/Deploy:** `eb245e7` · Heroku **v88** · origin synced.
- **Verifikasi:** file ter-serve memuat blok; data CGK realMT 205,8 ada. Catatan: browser yang terbuka saat deploy cache `?v=12` lama → hard-refresh sekali.
