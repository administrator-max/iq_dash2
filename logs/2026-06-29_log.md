# Change Log — 2026-06-29

> Aturan: setiap update tulis di sini (lihat `CLAUDE.md`).

## 16:25 WIB — Input realisasi GKL WELDED PIPE (MV MSC BERN V) + cek Neon quota_monitoring

### A. Cek Neon quota_monitoring (jawab: detail KAN tidak ada di mana pun)
- Konek ke Neon `quota_monitoring` (`tbl_realization`+`tbl_real_detail`, 149 baris, 20 company). **KAN/NCT = 0 baris** → realisasi mereka memang entry manual Operations (lot), bukan import PIB. Kolom customs kosong di export = inheren, bukan migrasi terlewat.
- **Temuan tambahan (belum ditindak):** tabel `realizations` iq_dash **dobel** — 285 baris vs Neon 149. Dua batch tumpang-tindih: `bulk-zip-import` (excel per-PIB, 13 Mei) + `migrationA` (`A:{pib}/programA`). **16 company** punya ~141 baris ekstra (GKL +59, MIN +23, BTS +9, dst). Kedua batch lengkap (tak ada field kosong). Hanya memengaruhi modal/export PIB (±2×), **bukan** KPI Total Realized (sumber RA berat). → Pending: dedup (destruktif, perlu konfirmasi + backup).

### B. Input realisasi GKL WELDED PIPE 387,603 (split per keputusan user)
- **Ubah (data):** GKL ERW PIPE OD≤140mm realMT=**23,991**; ERW PIPE OD>140mm realMT=**363,612**; keduanya pib 19/06/2026, cargoArrived=true, note "MV MSC BERN V". RA `berat` 721,26 → **1.108,863** (+387,603), realPct→0,46203.
- **Cara:** `PATCH /api/company/GKL` (shipments + ra) → `patchCompanySheets`. Lot utilMT=0 → guard anti-zeroing → **utilisasi tidak tersentuh** (tetap 2400, per-produk 500/800/1100).
- **Validasi:** realisasi ≤ utilisasi (23,991≤800; 363,612≤500). ✓
- **Tab/Sheet:** `company_shipments` (real_mt/pib/cargo_arrived/note), `ra_records` (berat/real_pct), `Change_Log`.
- **Commit/Deploy:** data-only (tanpa deploy).
- **Verifikasi (live):** GKL utilMT=2400 avail=0 (tetap); ERW lots realMT 23,991/363,612 pib 19/06 arrived; RA berat=1.108,863 realPct=0,46203. HTTP 200.
- **Catatan:** GKL punya baris PIB → modal "Realization Details" menampilkan PIB (bukan lot fallback), jadi ERW realisasi ini tampil di **drawer per-lot** & tab Util & Realization (via RA berat), bukan di modal PIB. arrivalDate RA dibiarkan (2026-03-04) — keterbatasan single-date.
