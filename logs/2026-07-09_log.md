# Log — 2026-07-09

## HH:MM WIB — Utilisasi Sales tidak ke-save untuk company pending-ber-ledger (IKM) — FIXED

**Keluhan:** Input Utilization lot di form Sales (IKM · GI ALLOY, PERTEK 4.150) → klik Simpan → tidak tersimpan (Available tetap 4.150, util 0).

**Investigasi (systematic-debugging, live via Claude-in-Chrome + curl + store dump):**
- WRITE **berhasil**: `PATCH /api/company/IKM` (shipments) → 200; `GET /api/company/IKM` (per-company, uncached) menampilkan lot util & utilizationMT dengan benar. Store (dibaca fresh) memang punya row `company_shipments` IKM GI ALLOY.
- Tapi **`GET /api/data`** (yang di-load dashboard) tetap util 0 / shipments `{}` — bahkan X-Cache=BUILD (rebuild) tetap 0. Jadi bukan cache/timing.
- **Root cause:** IKM `section=PENDING` tapi punya entry di `quotaLedger.json`. Di `_buildDataPayload`, ledger-company yang tidak ada di array `spi` **disintesis** jadi objek `spi` dengan **`shipments: {}` hardcoded** (server.js ~b.1244). Akibatnya `applyLedger` melihat `lotU=0` → util = ledgerUtil saja (0) → lot yang tersimpan **dibuang** dari tampilan bulk. Semua company pending-ber-ledger terdampak (IKM).

**Ubah:** di loop sintesis ledger (`_buildDataPayload`, server.js), bangun `shipMapFor` dari `shipRows` (lot nyata company itu) dan pasang ke `shipments:` objek sintesis, menggantikan `{}`. Sekarang `applyLedger` menghitung `lotU` dari lot tersimpan → utilisasi tampil.

**File:** `server.js` (fungsi `_buildDataPayload`, ~b.1240-1266). Frontend/store tidak berubah.

**Verifikasi (server lokal, data Sheets live):**
- Sebelum fix: `/api/data` IKM util=0 walau lot tersimpan.
- Sesudah fix: `/api/data` IKM util=222 (lot 222), avail GI ALLOY=3928, shipKeys=[GI ALLOY, SHEET PILE, SEAMLESS PIPE].
- End-to-end: PATCH util=500 → `/api/data` langsung util=500, avail=3650. ✅
- Data uji IKM (lot GI ALLOY) sudah di-revert ke util 0 / avail 4.150 (store rewrite langsung; hanya restore, bukan perubahan bisnis).

**Catatan:** ditemukan divergensi kecil stats-vs-ledger untuk IKM (stats obtained 2.002 vs ledger 4.150) — tidak memengaruhi tampilan (ledger yang menang di `/api/data`); dicatat untuk audit terpisah.
