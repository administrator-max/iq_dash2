# Log — 2026-07-13

## Sesi — PERTEK Perubahan release-date gate (MIN → Wear Plate, bukan GI Alloy)

- **Ubah:** Menambahkan *gate*: split PERTEK Perubahan yang **belum terbit** di-reverse ke
  PERTEK asal sampai tanggal terbit diisi. Seed hanya **MIN** (BORDES ALLOY ← GI ALLOY 353 MT →
  MIN tampil Wear Plate 600 MT, util 247, avail 353; GI Alloy total turun 353). Mekanisme umum:
  perusahaan lain ditambah dengan menambah baris di `lib/pendingRevisions.json`.
- **File/Tab:**
  - `lib/pendingRevisionGate.js` (baru, pure function + `test/pendingRevisionGate.test.js`, 8/8 pass)
  - `lib/pendingRevisions.json` (baru, seed MIN)
  - `server.js` — `applyLedger()` (reverse split + `co._pendingRevision`), `releasedMap`,
    endpoint `POST /api/company/:code/pertek-perubahan-release` (Sheets-only, validasi kode∈pending + format tanggal)
  - `lib/sheetsStore.js` — `TABLES` + `load()` tahan tab yang belum ada (recovery gated HTTP 400)
  - `public/js/13-rev-mgmt.js` — banner "PERTEK Perubahan belum terbit" + input tanggal terbit
  - Tab Sheets baru: `pertek_perubahan_release` (`code | release_date | updated_at`) — dibuat via
    `migration_work/add_pertek_perubahan_release_tab.js` (belum dijalankan; jalankan saat deploy)
  - Docs: spec + plan di `docs/superpowers/`
- **Commit/Deploy:** branch `feat/pertek-perubahan-gate` (5c319e2..decc9d9). **Belum di-deploy** —
  deploy (buat tab prod + `git push heroku main`) + entri versi Heroku menyusul.
- **Alasan:** MIN masih proses perubahan PERTEK (Wear Plate 600 → Wear Plate 246,8 + GI Alloy 353,2);
  dashboard tidak boleh menghitung GI Alloy sebelum PERTEK Perubahan terbit. Berlaku untuk semua perusahaan.
- **Verifikasi (lokal):** `node --test` gate 8/8 hijau; `/api/data` (Sheets mode, tab belum ada) HTTP 200,
  MIN `_ledgerObtainedByProd = {"BORDES ALLOY":600}`, log `[sheets] load: skipping missing tab(s)`;
  endpoint mengembalikan 400 untuk kode non-pending / tanggal invalid / body kosong (tanpa write).
  Whole-branch review (opus): merge-ready, tanpa temuan Critical/Important.
- **Pending saat deploy:** (1) `node migration_work/add_pertek_perubahan_release_tab.js` untuk membuat tab prod;
  (2) `git push heroku main`; (3) verifikasi live GI Alloy tanpa MIN (total −353) & alur isi tanggal terbit
  → split muncul lagi; (4) append entri deploy (hash + Heroku vNN) ke log ini.
