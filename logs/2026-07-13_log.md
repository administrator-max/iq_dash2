# Log — 2026-07-13

## Sesi — PERTEK Perubahan release-date gate (MIN → Wear Plate, bukan GI Alloy)

- **Ubah:** Menambahkan *gate*: split PERTEK Perubahan yang **belum terbit** di-reverse ke
  PERTEK asal sampai tanggal terbit diisi. Seed hanya **MIN** (BORDES ALLOY ← GI ALLOY 353 MT →
  MIN tampil Wear Plate 600 MT, util 247, avail 353; GI Alloy total turun 353). Mekanisme umum:
  perusahaan lain ditambah dengan menambah baris di `lib/pendingRevisions.json`.
- **File/Tab:**
  - `lib/pendingRevisionGate.js` (baru, pure function + `test/pendingRevisionGate.test.js`, 8/8 pass)
  - `lib/pendingRevisions.json` (baru, seed MIN)
  - `server.js` — `applyLedger()` (reverse split + `co._pendingRevision`), `releasedMap`,
    endpoint `POST /api/company/:code/pertek-perubahan-release` (Sheets-only, validasi kode∈pending + format tanggal)
  - `lib/sheetsStore.js` — `TABLES` + `load()` tahan tab yang belum ada (recovery gated HTTP 400)
  - `public/js/13-rev-mgmt.js` — banner "PERTEK Perubahan belum terbit" + input tanggal terbit
  - Tab Sheets baru: `pertek_perubahan_release` (`code | release_date | updated_at`) — dibuat via
    `migration_work/add_pertek_perubahan_release_tab.js` (belum dijalankan; jalankan saat deploy)
  - Docs: spec + plan di `docs/superpowers/`
- **Commit/Deploy:** branch `feat/pertek-perubahan-gate` (5c319e2..decc9d9). **Belum di-deploy** —
  deploy (buat tab prod + `git push heroku main`) + entri versi Heroku menyusul.
- **Alasan:** MIN masih proses perubahan PERTEK (Wear Plate 600 → Wear Plate 246,8 + GI Alloy 353,2);
  dashboard tidak boleh menghitung GI Alloy sebelum PERTEK Perubahan terbit. Berlaku untuk semua perusahaan.
- **Verifikasi (lokal):** `node --test` gate 8/8 hijau; `/api/data` (Sheets mode, tab belum ada) HTTP 200,
  MIN `_ledgerObtainedByProd = {"BORDES ALLOY":600}`, log `[sheets] load: skipping missing tab(s)`;
  endpoint mengembalikan 400 untuk kode non-pending / tanggal invalid / body kosong (tanpa write).
  Whole-branch review (opus): merge-ready, tanpa temuan Critical/Important.
## Deploy — PERTEK Perubahan gate LIVE

- **Deploy:** merge `feat/pertek-perubahan-gate` → `main` (FF ke `29f030b`), buat tab prod
  (`node migration_work/add_pertek_perubahan_release_tab.js` → tab `pertek_perubahan_release` dibuat),
  `git push heroku main` → **Heroku v112** (iq-dash).
- **Verifikasi (live, https://iq-dash-ce64ce1e5182.herokuapp.com/api/data, HTTP 200):**
  - MIN `_ledgerObtainedByProd = {"BORDES ALLOY":600}` (util 247, avail 353), tanpa GI ALLOY; `_pendingRevision` terisi.
  - **GI ALLOY total: Obtained 9.904 → 9.551 (−353), Available 5.052 → 4.699 (−353)**, util 4.852 (tetap).
  - Jumlah perusahaan GI ALLOY 15 → 14 (MIN keluar dari daftar).
- **Catatan:** un-gate MIN dilakukan lewat banner "PERTEK Perubahan belum terbit" (isi tanggal terbit)
  saat PERTEK Perubahan benar-benar terbit; belum diisi (MIN masih pending, sesuai instruksi).

## Fix — IKM (ledger-only) tidak muncul saat filter periode Juni/Juli

- **Gejala:** IKM (GI Alloy 4150, Seamless 2100, Sheet Pile 1750) hanya tampil di "All Time",
  hilang saat filter bulan Juni/Juli.
- **Akar masalah:** IKM adalah *synthesized ledger-only company* (ada di `lib/quotaLedger.json`,
  tidak ada di cycles/SPI) → objeknya dibuat dengan `cycles: []`. Period filter
  (`public/js/02-period-filter.js` `companyInPeriod`) mencocokkan tanggal **hanya dari cycles**;
  tanpa cycle → dikecualikan dari semua filter bulan spesifik. Ledger tidak menyimpan tanggal.
- **Fix (server-side, 1 titik):** synthesize loop `server.js` (~1285) sekarang membuat **synthetic
  cycle `Obtained #1`** untuk company ledger-only yang punya tanggal di
  `lib/ledgerCompanyDates.json` (baru; seed `{"IKM":"30/06/2026"}` — obtained 30 Juni per info user).
  Cycle: `releaseDate/spiDate/pertekDate = 30/06/2026`, `mt` & `products` diturunkan dari ledger
  (8000 = 4150+1750+2100) → konsisten dgn ledger. `canonicalObtained` tetap short-circuit ke
  `_ledgerObtained` di All Time (tidak double-count); util/avail (`scoped*ByProd`) memang tidak
  bergantung cycle. Umum untuk semua company ledger-only (tambah baris di JSON).
- **File:** `lib/ledgerCompanyDates.json` (baru), `server.js` (require + synthesize loop).
- **Verifikasi (lokal):** `/api/data` HTTP 200 → IKM.cycles = `[{Obtained #1, mt 8000, terbit 30/06/2026,
  3 produk}]`, `_ledgerObtained` 8000, available GI 4148 (ada lot util 2). Simulasi filter
  `companyInPeriod` untuk Juni → **match=true** (spiTerbit 30/06/2026 ∈ Juni).
- **Deploy:** commit `0a1902b` → **Heroku v113** + origin. Verifikasi live (setelah cache prewarm ~25s):
  `/api/data` IKM.cycles = `[Obtained #1, terbit 30/06/2026]` → IKM kini muncul di filter **Juni**.
  (Catatan: karena obtained 30 Juni, IKM tampil di filter Juni, bukan Juli.)
